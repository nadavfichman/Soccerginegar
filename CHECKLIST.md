# רשימת בדיקה לפני כל push ל-`main`

סעיפים 1, 2 ו-5 רצים אוטומטית ב-CI על כל push (`npm run check`, ר' `.github/workflows/smoke-test.yml` ו-`scripts/checklist-checks.js`) — אם נשכח לבדוק ידנית, ה-push עדיין ייכשל. סעיפים 3, 4, 6, 7 נשארים ידניים במכוון (שיפוט אנושי, או דורשים גישה בפועל ל-Supabase/לאתר החי) — הרשימה הזו היא התחליף הקל-משקל להם.

## 1. תחביר תקין (אוטומטי ב-CI, אפשר גם ידנית — לוקח שניות)

`index.html` מכיל JSX שמתקומפל **בדפדפן** בזמן ריצה (Babel Standalone) — שגיאת תחביר לא נתפסת עד שמישהו פותח את הדף ורואה מסך לבן. לבדוק **לפני** push:

```bash
node -e "
const babel = require('@babel/standalone'); // npm install --no-save @babel/standalone אם חסר
const fs = require('fs');
const html = fs.readFileSync('index.html', 'utf8');
const src = html.match(/<script type=\"text\/babel\"[^>]*>([\s\S]*?)<\/script>/)[1];
babel.transform(src, { presets: ['react'] });
console.log('OK');
"
```
אם זה זורק שגיאה — יש שגיאת תחביר, לא לדחוף.

## 2. אין כתיבה ישירה לטבלה שעוקפת RPC (אוטומטי ב-CI)

```bash
grep -n 'db\.from(' index.html
```
כל תוצאה חייבת להיות `.select(...)` בלבד. אם יש `.insert(`/`.update(`/`.delete(` — זו כתיבה שעוקפת את שכבת ה-RLS/הרשאות (ר' `CLAUDE.md`, "באגים משמעותיים" #3).

## 3. כל `db.rpc(...)` בודק שגיאה

חיפוש ידני: כל קריאת RPC ניהולית צריכה לעבור דרך `callAdmin(...)` (לא `db.rpc(...)` ישירות) — אחרת כשל בשרת ייבלע בשקט (ר' "באגים משמעותיים" #1 ב-`CLAUDE.md` — זה בדיוק מה שהסתיר את הבאג המקורי).

```bash
grep -n 'db\.rpc(' index.html
```
כל שורה שאינה בתוך `callAdmin`/`api.*`/מטפלת מפורשות ב-`error` — לבדוק שוב.

## 4. אם יש שינוי סכימה/RPC ב-Supabase

- לעדכן את `supabase/functions.sql` ו/או `supabase/schema_additions.sql` בהתאם (לשמור על תיעוד הגרסה מסונכרן)
- לוודא שברור למשתמש **בדיוק** אילו SQL צריך להריץ ב-Supabase **לפני** שהקוד שמסתמך עליהם עולה לפרודקשן — אחרת יש חלון זמן שבו הקוד "שבור" (קרה בפועל פעם אחת בפרויקט הזה, ר' היסטוריית שיחה)

## 5. `manifest.json` תקין (אוטומטי ב-CI)

```bash
python3 -c "import json; json.load(open('manifest.json')); print('OK')"
```

## 6. בדיקת עשן ידנית (אחרי שה-push כבר חי)

לפחות אחד מהשניים, לפי מה שהשתנה:
- **שינוי שחקן/הרשמה:** להיכנס כשחקן, לוודא שהמסך הרגיל עולה (לא תקוע ב"השלמת פרטים")
- **שינוי ניהול:** להיכנס כאדמין, לבדוק פעולה אחת שרלוונטית לשינוי (למשל: אישור בקשה, הוספת משחק, סימון נוכחות)

## 7. בדיקת `git status` לפני commit

לוודא שלא נכנס בטעות שום קובץ עם סיסמאות/מפתחות אמיתיים (מעבר למפתח ה-anon הציבורי שכבר קיים בקוד ומיועד להיות ציבורי).
