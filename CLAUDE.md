# כדורגל גניגרי ושות' — תיעוד ארכיטקטורה

אפליקציית רישום והרשמה למשחקי כדורגל של קבוצה חובבנית (~70 חברים). קובץ יחיד, בלי build step, מתארח ב-GitHub Pages, עם Supabase כ-backend.

## מבנה הפרויקט

- **`index.html`** — כל האפליקציה: React 18 + Babel Standalone (מתקומפל בדפדפן, בלי build), Supabase JS client, SheetJS (ייצוא אקסל). קובץ אחד גדול, בכוונה — שומר על הפשטות של "עריכה ישירה → git push → חי מיד".
- **`manifest.json`** — PWA manifest. `display: "browser"` בכוונה (לא `"standalone"`) — ר' הסבר בסעיף PWA למטה.
- **`sw.js`** — Service Worker מינימלי, ללא caching (נדרש טכנית לקריטריון ההתקנה, לא בשימוש בפועל).
- **`icons/`** — אייקוני PWA (192/512px), עם שם הקבוצה כתוב עליהם.
- **`supabase/`** — תיעוד גרסה של כל ה-SQL (פונקציות, RLS, תוספות סכימה). ר' `supabase/README.md` למגבלות התיעוד.

## Backend — Supabase

**טבלאות:** `players`, `player_requests`, `admins`, `admin_requests`, `games`, `registrations`, `settings`, `login_attempts`, `admin_invites`, `admin_pin_resets`.

**RLS:** כל הטבלאות הציבוריות נעולות ל-SELECT בלבד (`to public using (true)`). **כל כתיבה** עוברת אך ורק דרך פונקציות RPC מסוג `SECURITY DEFINER` שמאמתות הרשאה בצד השרת (ר' `supabase/functions.sql`). `settings`/`login_attempts` חסומות לגמרי (RLS מופעל, אין אף מדיניות).

**אין migrations מנוהלות** — שינויי סכימה מורצים ידנית ב-SQL Editor של Supabase. `supabase/*.sql` הוא "צילום מצב", לא היסטוריה כרונולוגית.

## מודל הרשאות

שלוש רמות, **בלי קשר ל-Supabase Auth**:
1. **שחקן** — מזוהה דרך Supabase Auth (אימות מייל, `signInWithOtp`). ה-`auth.uid()` מקושר לשורה ב-`players.auth_user_id`.
2. **מנהל (admin)** — נכנס עם טלפון + PIN אישי (`check_login`). אין לו session Auth אמיתי — רק אישור שנבדק דרך RPC ונשמר ב-state של React (`cred`, ברמת ה-`App` כדי לשרוד מעבר בין מסך שחקן/ניהול).
3. **אדמין-על (super)** — נכנס עם סיסמה יחידה, שמורה מוצפנת (bcrypt, דרך `pgcrypto`) ב-`settings.super_pw` מ-migration 0005 — לא בטקסט גלוי. `check_login` משווה דרך `crypt()`, `update_settings` מצפין סיסמה חדשה דרך `crypt(..., gen_salt('bf'))` לפני שמירה. שינוי הסיסמה דורש **גם** אימות מייל אמיתי ל-`nadavfichman@gmail.com` (`update_settings_secure`), לא רק את הסיסמה הנוכחית.

**כל קריאת RPC ניהולית** מצרפת בשקט את האישורים השמורים (`cred.pw` או `cred.phone`+`cred.pin`) ועוברת דרך `callAdmin()` שחושף שגיאות (לא בולע אותן בשקט — זה בדיוק מה שהסתיר את הבאג המקורי, ר' "באגים משמעותיים" למטה).

## היררכיית עדיפות בהרשמה למשחקים

`tierRank()` + `sortRegs()` ב-`index.html`:

```
גניגרי+ותיק > גניגרי > ותיק > רגיל
```

בתוך כל שכבה — **מספר ההגעות המצטבר בפועל** (`attendanceCounts`, לא רק הרשמה) שובר שוויון: מי שהגיע יותר, מדורג גבוה יותר בתוך אותה שכבה. **אין סף/תקרה קבועים** — זה במכוון דינמי (הוחלט אחרי שהוסבר שסף קבוע "נשחק" ברגע שכולם עוברים אותו).

**נעילת נוכחות (`attendance_locked` על `games`):** סימוני "הגיע" (`registrations.attended`) ניתנים לעריכה חופשית כל עוד המשחק "פתוח" — אבל הם **נספרים ב-`attendanceCounts` רק אם המשחק נעול**. פתיחה מחדש של משחק נעול מוציאה את ההגעות שלו מהספירה החיה **מיד**, לכל מי שרשום שם, גם משחקים ישנים — זה במכוון (ר' שיחה עם המשתמש), לא באג.

## "לא אגיע" (game_declines)

מ-migration 0006: שחקן יכול לסמן במפורש שהוא **לא** מגיע למשחק (לא רק להימנע מהרשמה) — כדי שלאדמין יהיה נתון אמיתי כמה אנשים בפועל לא יגיעו (למשל 10 סירובים מפורשים הם איתות ברור להזמין חברים מבחוץ, מה שאי-תגובה שקטה לא נותנת). טבלה נפרדת לגמרי מ-`registrations` — מושג שונה ("לא רוצה מקום", לא "רוצה מקום") — ולכן `sortRegs`/`tierRank`/`attendanceCounts` (`logic.js`) לא נוגעים בזה כלל.

**הדדיות:** `decline_game` מוחקת הרשמה קיימת; `register_for_game` (עודכנה במיוחד בשביל זה) מוחקת decline קיים. אי אפשר להיות גם "מגיע" וגם "לא מגיע" באותו זמן.

**זמינות:** כפתור "לא אגיע" ב-`GameCard` זמין רק כשההרשמה פתוחה (`isOpen`) — בדיוק כמו כפתור "מגיע ✓", לא לפני ולא אחרי פרסום הרשימה.

**תצוגת אדמין:** `AdminGameRow` מציג מספר ורשימת שמות של מי שסימן "לא מגיע" לכל משחק (קריאה ישירה מהטבלה, כמו registrations — אין RPC לקריאה, רק SELECT פתוח דרך policies.sql).

## PWA — למה `display: "browser"` ולא `"standalone"`

**זו לא טעות.** התקנת PWA מלאה (`standalone`) גורמת לכרום/אנדרואיד לייצר WebAPK שעובר סריקת Google Play Protect — וזו חסמה חלק ממשתמשי אנדרואיד עם אזהרת "אפליקציה לא בטוחה". שינוי ל-`"browser"` מוריד את האתר מקריטריוני ההתקנה המלאה של כרום, כך ש"הוסף למסך הבית" יוצר קיצור-דרך פשוט (בלי WebAPK, בלי Play Protect) — במחיר פס כתובת דק שנשאר גלוי. הוחלט במכוון לטובת 0% חסימות על פני "מראה נקי" מלא. אם ירצו בעתיד גם וגם — הפתרון היחיד האמיתי הוא פרסום רשמי ב-Google Play (TWA דרך PWABuilder), לא שינוי קוד.

## לוג פעולות ניהול (audit log)

`log_admin_action` (RPC, write-only, נגיש רק מתוך פונקציות ניהול אחרות — `revoke all ... from public`) שומר ל-`admin_audit_log` (בלי מדיניות SELECT) כל פעולת ניהול משמעותית: אישור/דחיית/שקילה-מחדש של בקשה, הוספה/מחיקה/עריכה של שחקן, יצירה/מחיקה/פרסום של משחק, הוספה/הסרה/עריכת הרשמה, סימון/נעילת נוכחות, ושינוי הגדרות-על (בלי לרשום סיסמאות בפועל). המטרה: אם משהו נמחק/שונה בטעות, אפשר לדעת מי עשה את זה ומתי — נקרא ישירות דרך שאילתת SQL ב-Supabase Dashboard, אין לו UI באפליקציה (כמו `client_errors`).

## ניטור שגיאות קליינט

`log_client_error` (RPC, write-only, ר' `supabase/functions.sql`) שומר ל-`client_errors` (בלי מדיניות SELECT — כמו `settings`) כל שגיאה לא צפויה: קריסת רינדור (`ErrorBoundary` ב-`index.html`), שגיאת JS גלובלית (`window.onerror`/`unhandledrejection`), וכל כישלון `db.rpc` שעובר דרך `callAdmin`. המטרה: לדעת שקרתה תקלה אצל משתמש בלי לחכות שהוא ידווח — בדיוק המחלקה של הבאג ב-`get_my_identity` למטה, שהתגלה רק כי הפסקנו לבלוע שגיאות RPC בשקט. `ErrorBoundary` גם מציג הודעה ידידותית + כפתור רענון במקום מסך לבן ריק.

**צפייה ביומן:** כפתור "📋 יומן שגיאות" ב-`SuperAdminPanel` (`ClientErrorLog`) קורא ל-`get_client_errors` (אדמין-על בלבד) ומציג את 200 השגיאות האחרונות בתוך האפליקציה עצמה — לא נטען אוטומטית, רק בלחיצה, כדי לא להעמיס בקשה מיותרת בכל טעינת מסך אדמין.

## אימות מייל לשחקן — קוד בן 6 ספרות, לא קישור-ללחיצה

`JoinScreen` שלח בעבר קישור-קסם (magic link, `emailRedirectTo`) — אבל קישורים נופלים קורבן לסריקה אוטומטית של Gmail (Safe Browsing מבקר בקישור לפני שהמשתמש לוחץ עליו, "שורף" את השימוש החד-פעמי). התוצאה: משתמש תקוע בלולאה אין-סופית בחזרה למסך ההצטרפות, בלי שום הסבר (ר' באג #4 למטה). תוקן במעבר לקוד ידני: `signInWithOtp({ email })` (בלי `emailRedirectTo`) ואז `verifyOtp({ email, token: code, type: "email" })` — אין קישור בכלל, אז אין מה לסרוק מראש.

**תלות בהגדרה ב-Supabase (לא בקוד):** Authentication → Email Templates → Magic Link **חייבת** להציג את `{{ .Token }}` ו**לא** לכלול קישור-ללחיצה (`<a href="{{ .ConfirmationURL }}">`) — אחרת הבעיה חוזרת, כי הקישור עדיין יופיע במייל וייסרק. זו לא נשלטת דרך `supabase/` (אין לזה SQL), רק דרך דשבורד ה-Auth.

`authError` (זיהוי שגיאת redirect ב-URL) נשאר כ-fallback למקרה שמישהו עדיין ילחץ על קישור ישן מלפני השינוי הזה, אבל אינו עוד הנתיב הראשי.

## באגים משמעותיים שנמצאו ותוקנו (היסטוריה, לידע future-session)

1. **`get_my_identity` (42804 datatype mismatch)** — `select *` כלל גם עמודת מיון פנימית (`pr`) שלא הייתה חלק מהחתימה המוצהרת של הפונקציה → שגיאה בכל קריאה → `identity` תמיד `null` → כל שחקן (גם מאושר) תמיד ראה מסך הצטרפות. **זה היה כנראה הבאג האמיתי** מאחורי "שחקן תקוע בהרשמה", לא הבעיה המקורית שחשבנו שזו ב-`submit_player_request`. אותר רק כי הקליינט הפסיק לבלוע שגיאות RPC בשקט.
2. **`cred` (אישורי ניהול) התאפסו במעבר מסך שחקן↔ניהול** — היו ב-state מקומי של `AdminView` (מתאפס בכל unmount), בעוד `role` היה ב-state של `App` (שורד). כל פעולת ניהול נכשלה בשקט אחרי מעבר מסכים. תוקן ע"י הרמת `cred` ל-`App`.
3. **RLS פתוח לגמרי** (`USING true` לכל הפעולות, כל הטבלאות) — כל מי שידע לפתוח קונסולת דפדפן יכול היה לכתוב/למחוק כל דבר, כולל לאשר את עצמו כשחקן/למחוק שחקנים. תוקן כמתואר למעלה.

**לקח כללי:** `db.rpc(...)` בקוד שלא בודק `error` הוא נקודת כשל שקטה קלאסית באפליקציה הזו — כל קריאת RPC חדשה צריכה לעבור דרך `callAdmin()` (לפעולות ניהול) או לבדוק `error` במפורש.

## סביבת פיתוח

- אין build step, אין npm/node בפרויקט עצמו — עריכה ישירה של `index.html`.
- לפני כל push: ר' `CHECKLIST.md` (תחביר JSX, אין כתיבה ישירה לטבלה, כל RPC בודק שגיאה, וכו').
- `main` הוא הענף החי (GitHub Pages פרוס ממנו). אין סביבת staging נפרדת — שינויים נבדקים ידנית בפרודקשן.
